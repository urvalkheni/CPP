#include<iostream>
#include<bits/stdc++.h>
using namespace std;
void explainUSet(){
    /*
    lower_bound and uper_bound function
    does not work rest all the fucntion are same
    as above, it does not stores any particular order it has a better complexity
    than set in most cases, expect some when collision happens    
    */
}
int main(){
    explainUSet();
    return 0;
}